{
	"$schema": "https://schema.tauri.app/config/3.0.0-alpha.2",
	"build": {
		"beforeBuildCommand": "bun run build:sidecar && bun run build:cli && bun run build:frontend"
	},
	"bundle": {
		"externalBin": ["binaries/sidecar", "binaries/nisi-cli"],
		"macOS": {
			"signingIdentity": "-",
			"entitlements": "./entitlements.plist"
		}
	}
}
