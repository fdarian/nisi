---
"@repo/desktop": patch
---

Add ⌘[/⌘] (Ctrl on non-mac) to step back/forward through the Files Changed pane's file focus history, browser-style — explicit selections push, scroll drift replaces the current entry in place, and stale paths from a PR update are skipped rather than followed.
