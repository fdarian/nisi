---
"@repo/desktop": patch
---

Sync the selected file to the diff pane's scroll position, so the tree highlight and `r` (mark reviewed) follow whichever file is on screen while scrolling, not just `j`/`k`/click.
