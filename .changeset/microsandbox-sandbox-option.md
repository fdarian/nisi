---
"@repo/desktop": minor
---

Agents can now run in microsandbox VMs instead of locally. Select "Microsandbox" in Settings to mount your repo into a VM and run Claude Code, Codex, or OpenCode there; Pi stays local. The microsandbox runtime is downloaded on first use.
