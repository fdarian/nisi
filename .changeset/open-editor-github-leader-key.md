---
"@repo/desktop": patch
---

Add `o` then `e` to open the selected file in your preferred editor, and `o` then `g` to open the pull request in GitHub — set a preferred editor from Settings (⌘,) or the first press of `o e` will offer to pick one.
