---
"@repo/desktop": patch
---

The walkthrough agent narrates only what matters instead of linking every changed hunk, and explores the worktree itself rather than working from a pre-truncated patch dump. Files it skipped are listed under the walkthrough — click one to see its hunks.
