---
"@repo/desktop": patch
---

The "About nisi" menu item now opens a native-styled About window instead of the plain macOS panel, showing the app version and the exact commit the build was produced from, linked to its page on GitHub.
