---
"@repo/desktop": patch
---

Pressing Escape in the chat composer now returns focus to the file list, so `j`/`k` immediately work to cycle files again. A second Escape closes the chat panel, and ⌘J now refocuses the composer instead of closing the panel when it's open but unfocused.
