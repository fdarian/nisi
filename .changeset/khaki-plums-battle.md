---
"@repo/walkthrough": patch
"@repo/desktop": patch
---

Switch the walkthrough agent's buffer from a JSON string to a markdown document, and add a `read_walkthrough` tool so the agent can re-sync before editing.
